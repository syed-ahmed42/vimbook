#include <stdlib.h>
#include <stdio.h>

void hello(char *name);

int main(void)
{
    int num = 2;
    hello("David");
    num += 1;
    return 0;
}

void hello(char *name)
{
    scanf("%s", &name);
    printf("Hello %s\n", name);
}
